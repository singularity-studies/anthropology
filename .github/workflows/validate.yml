name: Validate anthropology research scaffold

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate ethics-first scaffold
        run: python scripts/validate.py
      - name: Run regression tests
        run: python -m unittest discover -s tests -v
