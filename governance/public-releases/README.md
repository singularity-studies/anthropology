# Public-release Records

No release is authorized.

Future JSON records must validate against `schemas/public-release.schema.json`, identify one artifact under `public/`, bind its exact SHA-256 digest, and record passed disclosure and de-identification reviews. Records must use non-identifying responsible-role labels rather than participant or reviewer personal information.
